FROM ubuntu:24.04
RUN apt-get update && \
    apt-get install -y --no-install-recommends bash coreutils findutils grep sed shellcheck && \
    rm -rf /var/lib/apt/lists/*
WORKDIR /hyunarch
ENV HYUNARCH_DRY_RUN=1
CMD ["bash"]
